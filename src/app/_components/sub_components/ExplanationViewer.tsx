"use client"

interface ExplanationViewerProps {
    explanation: string;
    isLoading: boolean;
    onRetry?: () => void;
}

const ExplanationViewer = ({}:ExplanationViewerProps) => {
    return (

    )
}

export default ExplanationViewer;